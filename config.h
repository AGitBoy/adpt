#ifndef CONFIG_H
#define CONFIG_H

// Default string used when adapter is plugged in
#define ONSTR "Plugged in"

// Default string used when adpater isn't plugged in
#define OFFSTR "Unplugged"

#endif
